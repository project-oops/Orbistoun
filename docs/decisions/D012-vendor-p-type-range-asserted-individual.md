# D012 - Vendor `p_type` range asserted; individual constants are not

**decided** · 2026-08-19

The range is enough to route a segment to vendor handling and to report an unparsed
one honestly. Specific values stay unasserted pending verification against a public
reference - a wrong constant that silently mis-parses is worse than "not handled".

