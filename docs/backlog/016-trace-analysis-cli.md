# Trace analysis CLI

Query a recorded trace: calls by site, first unresolved call before a hang, thread
interleaving around an event. Partly answered since it was written - a run now prints the
last calls before a fault, the argument dumps, and a ranked finding list - but there is
still no way to ask a question of a persisted trace that the run did not already answer.

