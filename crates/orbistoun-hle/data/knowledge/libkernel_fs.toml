library = "libkernel_fs"

[[function]]
name = "sceKernelClose"
arity = 1
purpose = "Closes a descriptor."
returns = "status"
edge_cases = [
    "Closing a standard stream succeeds and does nothing. A guest tidying up on exit should not be told it failed, and actually closing the host's error stream would take the fault reporter with it.",
    "Measured on hardware: obSCEne `040-file/close-rejects-bad-fd` reported pass, value 0x80020009. The check names the condition; see `data/hardware/ps5-full.txt` and `data/hardware/ps5-imports.txt` in the sibling conformance-probe repository.",
    "Measured on hardware: obSCEne `140-oracle/error-codes` reported pass, value 0x5. The check names the condition; see `data/hardware/ps5-full.txt` and `data/hardware/ps5-imports.txt` in the sibling conformance-probe repository.",
    "Measured on hardware: obSCEne `061-imports/unbound-import-is-callable` reported skip - this import bound, so there is no unbound one here to try calling. The check names the condition; see `data/hardware/ps5-imports.txt` in the sibling conformance-probe repository.",
]
found_by = "generated"
known_by = "guest-observed"
assumptions = [
    "Modelled on the POSIX call of the same shape. The correspondence is inferred from the name and the guest's usage, never verified against the target library.",
    "Which errno values the target returns, and on which conditions, is unverified.",
]
found_on = "2026-08-21"

[[function]]
name = "sceKernelDebugOutText"
arity = 2
purpose = "Writes a guest debug string to the system log."
returns = "status"
edge_cases = [
    "Captured rather than stubbed because obSCEne writes every report record here unconditionally, as a second destination beside its file sink and socket - a build whose other channels produce nothing still emits a full report through the kernel log. Forwarded to the same host stream the write path uses (D170), so orbistoun captures that report too.",
    "The channel argument is not modelled: every channel lands on the one stream.",
    "Answers a status and not a byte count, so success is a plain 0.",
    "The string read is bounded. An unterminated one is a guest bug and the scan must not run off the end of memory chasing a NUL that is not coming.",
]
known_by = "assumed"
assumptions = ["That both of the console's channels are the operator's log. That is why the channel argument is ignored, and it is inferred rather than measured - a run that showed the two channels going to different places would make ignoring it wrong."]
found_on = "2026-09-01"

[[function]]
name = "sceKernelLseek"
arity = 3
purpose = "Moves the read position. `whence` takes the published POSIX values - set 0, current 1, end 2."
returns = "count"
edge_cases = ["Measured on hardware: obSCEne `040-file/lseek-rejects-bad-fd` reported pass, value 0xffffffff80020009. The check names the condition; see `data/hardware/ps5-full.txt` and `data/hardware/ps5-imports.txt` in the sibling conformance-probe repository."]
found_by = "generated"
known_by = "assumed"
assumptions = [
    "Inferred from the name and the POSIX call it resembles. No guest has exercised it.",
    "Whether the whence constants match the POSIX values is unverified.",
]
found_on = "2026-08-21"

[[function]]
name = "sceKernelMkdir"
arity = 2
purpose = "Creates a directory in the guest's sandboxed filesystem."
returns = "status"
known_by = "assumed"
found_on = "2026-09-01"

[[function]]
name = "sceKernelOpen"
arity = 3
purpose = "Opens a path, answering a descriptor."
returns = "handle"
edge_cases = [
    "**Read-only regardless of the flags asked for**, and the flags are not honoured. A write mode would be a write into the user's own title directory; refusing the open outright would stop a guest that only wanted to read.",
    "A file never gets descriptor 0, 1 or 2 - handing it descriptor 1 would make its writes appear as program output.",
    "Lowest free descriptor, which is what a caller expects and makes a leak visible as a number that climbs.",
    "Measured on hardware: obSCEne `040-file/open-rejects-missing` reported pass, value 0x80020002. The check names the condition; see `data/hardware/ps5-full.txt` and `data/hardware/ps5-imports.txt` in the sibling conformance-probe repository.",
    "Measured on hardware: obSCEne `040-file/open-rejects-null` reported pass, value 0x8002000e. The check names the condition; see `data/hardware/ps5-full.txt` and `data/hardware/ps5-imports.txt` in the sibling conformance-probe repository.",
]
found_by = "generated"
known_by = "guest-observed"
assumptions = [
    "Modelled on the POSIX call of the same shape. The correspondence is inferred from the name and the guest's usage, never verified against the target library.",
    "Which errno values the target returns, and on which conditions, is unverified.",
]
found_on = "2026-08-21"

[[function]]
name = "sceKernelPread"
arity = 4
purpose = "Reads from a descriptor at an offset without moving its position."
edge_cases = [
    "Arity 4, the shape POSIX pread has, which this crate implements and orbistoun-libc declares at four.",
    "Shares descriptor::read_at with the POSIX form - reading at an offset without moving the position is the whole point, and a seek/read/seek-back is a race with extra steps.",
    "Failure differs from the POSIX form as it does for stat: this answers the vendor EBADF sign-extended, which obSCEne measured for read/lseek/write against a bad descriptor (D439), not -1.",
]
known_by = "guest-observed"
found_in = ["PPSA02664"]
found_on = "2026-09-03"

[[function]]
name = "sceKernelRead"
arity = 3
purpose = "Reads from a descriptor."
returns = "count"
edge_cases = [
    "Reading a standard stream answers zero rather than blocking on the host's own input - a guest reading stdin would otherwise hang the worker on a terminal nobody is typing into, and a hang costs the whole trace.",
    "Measured on hardware: obSCEne `018-relational/file-position-tracks-reads` reported pass, value 0x20. The check names the condition; see `data/hardware/ps5-full.txt` and `data/hardware/ps5-imports.txt` in the sibling conformance-probe repository.",
    "Measured on hardware: obSCEne `040-file/read-rejects-bad-fd` reported pass, value 0xffffffff80020009. The check names the condition; see `data/hardware/ps5-full.txt` and `data/hardware/ps5-imports.txt` in the sibling conformance-probe repository.",
]
found_by = "generated"
known_by = "guest-observed"
assumptions = [
    "Modelled on the POSIX call of the same shape. The correspondence is inferred from the name and the guest's usage, never verified against the target library.",
    "Which errno values the target returns, and on which conditions, is unverified.",
]
found_on = "2026-08-21"

[[function]]
name = "sceKernelStat"
arity = 2
purpose = "Reports what a guest is told about a path."
edge_cases = [
    "The vendor-named form of POSIX stat. The success path and the struct stat generation are shared with it (D374); only failure differs - POSIX answers -1, this answers a 0x8002_00xx vendor code, as sceKernelMkdir beside it does.",
    "ENOENT for a path nothing answers: facts_of returns nothing only when neither a host file nor a mount point is behind the name.",
]
known_by = "guest-observed"
found_in = [
    "PPSA02664-app0",
    "PPSA02664",
]
found_on = "2026-09-03"
note = "Called 1 time(s) across the recorded corpus; nothing beyond the name and the library is established."

[[function]]
name = "sceKernelFstat"
arity = 2
purpose = "Reports what a guest is told about the file behind an open descriptor."
edge_cases = [
    "The vendor-named form of POSIX fstat. The success path and the struct stat generation are shared with fstat and sceKernelStat (D374, D525); only failure differs - POSIX answers -1, this answers a 0x8002_00xx vendor code. A descriptor with no open file behind it is EBADF; a buffer that cannot be written is EFAULT.",
    "fstat and sceKernelStat were both served but this libkernel name was not wired, so a guest calling it read whatever its stack held where an unwritten struct stat should be - the D171 out-parameter failure.",
]
known_by = "guest-observed"
found_in = ["PPSA04263-app0"]
found_on = "2026-09-21"
note = "PPSA04263 (Grand Theft Auto V) calls it by the libkernel name; wiring it to the existing fstat logic moved the run FURTHER (+6 calls, +1 import, worklog 761)."

[[function]]
name = "sceKernelWrite"
arity = 3
purpose = """
Writes bytes to a descriptor. **The call that decides whether a conformance probe can
report at all.**

A probe reports by writing to standard output. One loader examined implements this purely
as a filesystem call - it requires a real opened descriptor and refuses descriptor 1 - and
that single choice is why it cannot emit a report (D170).
"""
returns = "count"
edge_cases = [
    "**Descriptors 1 and 2 land on the host's *error* stream, never its output stream.** The worker's stdout carries its protocol as newline-delimited JSON, and guest bytes interleaved into that would break the reader permanently - the same reasoning the fault reporter already follows.",
    "Flushed per write, because a probe's output is only useful if it survives the guest faulting immediately afterwards, which is the ordinary case here.",
    "Writing to a file is refused: files are opened read-only, so it would be a write into the user's own title directory.",
    "Measured on hardware: obSCEne `000-boot/write-rejects-bad-fd` reported pass, value 0xffffffff80020009. The check names the condition; see `data/hardware/ps5-full.txt` and `data/hardware/ps5-imports.txt` in the sibling conformance-probe repository.",
]
found_by = "generated"
known_by = "guest-observed"
assumptions = [
    "Modelled on the POSIX call of the same shape. The correspondence is inferred from the name and the guest's usage, never verified against the target library.",
    "Which errno values the target returns, and on which conditions, is unverified.",
]
found_on = "2026-08-21"
note = "Prompted by findings from the obSCEne probe running under other loaders. Behavioural observation of what breaks a report, not anything read from another project's source."

[[function.arguments]]
name = "fd"
kind = "u32"
note = "1 and 2 are standard output and error; anything else must have been opened"

[[function.arguments]]
name = "buffer"
kind = "ptr"
note = "guest memory, read only"

[[function.arguments]]
name = "length"
kind = "usize"
note = "bytes to take"
