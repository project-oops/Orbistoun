library = "libSceUserService"

[[function]]
name = "sceUserServiceGetGamePresets"
found_in = ["PPSA02664-app0"]
found_on = "2026-09-03"
note = "Called 1 time(s) across the recorded corpus; nothing beyond the name and the library is established."

[[function]]
name = "sceUserServiceGetLoginUserIdList"
arity = 1
purpose = "Reports which users are currently signed in."
edge_cases = [
    "IMPLEMENTED as one identifier: orbistoun signs exactly one user in, so the list of signed-in users has exactly one entry, and writing it needs no guess about the structure. It answers the same identifier sceUserServiceGetInitialUser does, through one shared accessor, so the two cannot disagree about who is signed in.",
    "WHAT IS NOT WRITTEN is the deliberate part. Anything past the first identifier is left as the caller had it, because zero-filling a tail would invent a length nothing has measured. A caller that does not clear the structure first reads its own stale bytes as extra users - a wrong answer that is visible, where a guessed length is a wrong answer that is not.",
]
known_by = "assumed"
assumptions = [
    "How many identifiers the structure holds. The symbol has never resolved on any leg obSCEne has run - 130-layout/user-service-layout skips with libSceUserService symbols not available in this context - so nothing has measured its length.",
    "Whether a console reports more than one signed-in user at once, and if so whether unused slots carry a sentinel. orbistoun models a single signed-in user, so the question has not arisen here.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-09-03"
note = "Called 1 time(s) across the recorded corpus; nothing beyond the name and the library is established."
