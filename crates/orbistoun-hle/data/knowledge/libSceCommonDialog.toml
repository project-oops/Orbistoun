library = "libSceCommonDialog"

[[function]]
name = "sceCommonDialogInitialize"
purpose = "Initialises the common-dialog subsystem. Three retail Unity titles call it early and check the return."
edge_cases = [
    "Answering 0 lets PPSA02664 proceed 123 further distinct imports and 402,745 further calls; answering a code with the high bit set stops it dead, printing 'sceCommonDialogInitialize() failed' and calling exit. Run both ways on 2026-09-10 (D670).",
    "PPSA02664, PPSA03416 and PPSA25872 all stop here and all three ship and run on a console, so it succeeds there. That is what makes 0 the guest-observed answer rather than a guess.",
    "NOT measured. obSCEne's 130-layout/common-dialog reads sceCommonDialogInitialize|resolved|0x0 and skips - 'not available in this context' - and 900-surface/dialog reports every libSceCommonDialog symbol absent on the package leg. Requested as obscene REQ-20260910T0600Z-a3f7.",
    "orbistoun now answers 0 (D678). a3f7 resolved that the value cannot be measured in a title context - libSceCommonDialog is not linked into a title process by default and its symbol is absent/unreachable without sceSysmoduleLoadModule first - so the measurement path is closed and guest-observed is the ceiling of the evidence. Implementing the guest-observed answer is the honest resolution rather than a permanent refusal of a call three shipping titles pass. The return is a pure status with no out-parameter, so 0 is one value and not a value plus an unmeasured structure (contrast the Earthion mapper gate, D677).",
]
known_by = "guest-observed"
assumptions = ["That the console returns exactly 0 rather than another non-negative value. The guest only tests the sign, so any of them would look the same from here."]
found_in = [
    "PPSA02664-app0",
    "PPSA03416-app0",
    "PPSA25872-app0",
]
found_on = "2026-09-03"
note = "Called 1 time(s) across the recorded corpus; nothing beyond the name and the library is established."
