library = "libScePosix"

[[function]]
name = "_close"
arity = 1
purpose = "The underscored spelling of `close`, and the same function pointer. Declared 2026-08-29."
returns = "status"
edge_cases = ["FreeBSD's own underscored spelling, which its C library uses internally so a program replacing the public name does not break the library's own calls. The payloads import both, and they are one function - so both reach one implementation and cannot answer differently."]
found_by = "static"
known_by = "assumed"
assumptions = ["That the underscored and public spellings are one function on this platform, as they are on FreeBSD."]
found_in = ["ftpsrv"]
found_on = "2026-08-29"

[[function]]
name = "_open"
arity = 3
purpose = "The underscored spelling of `open`, and the same function pointer. Declared 2026-08-29."
returns = "handle"
edge_cases = ["FreeBSD's own underscored spelling, which its C library uses internally so a program replacing the public name does not break the library's own calls. The payloads import both, and they are one function - so both reach one implementation and cannot answer differently."]
found_by = "static"
known_by = "assumed"
assumptions = ["That the underscored and public spellings are one function on this platform, as they are on FreeBSD."]
found_in = ["ftpsrv"]
found_on = "2026-08-29"

[[function]]
name = "_read"
arity = 3
purpose = "The underscored spelling of `read`, and the same function pointer. Declared 2026-08-29."
returns = "count"
edge_cases = ["FreeBSD's own underscored spelling, which its C library uses internally so a program replacing the public name does not break the library's own calls. The payloads import both, and they are one function - so both reach one implementation and cannot answer differently."]
found_by = "static"
known_by = "assumed"
assumptions = ["That the underscored and public spellings are one function on this platform, as they are on FreeBSD."]
found_in = ["ftpsrv"]
found_on = "2026-08-29"

[[function]]
name = "accept"
arity = 3
purpose = "Takes the next connection, answering a descriptor for it."
returns = "handle"
edge_cases = [
    "Blocks, as the interface does. A guest that calls this with nothing connecting waits, and the run's own time limit is what ends it.",
    "A null address argument is accepted and nothing is written, which is what a caller wanting only the connection passes.",
    "Declared in this library by inference rather than measurement: eight of its siblings were measured being imported from here by a title, and this one was not. A title connects rather than listens, so nothing in the corpus asks for it.",
]
known_by = "published"
cites = "POSIX.1-2008 accept(2)."
assumptions = ["That this library is where the platform exports it."]
found_in = [
    "klogsrv",
    "ftpsrv",
    "shsrv",
]
found_on = "2026-08-29"

[[function]]
name = "bind"
arity = 3
purpose = "Records where a socket is to listen."
returns = "status"
edge_cases = [
    "Remembered rather than performed: the host binds and listens in one call, so listen uses what this recorded. Binding here and rebuilding at listen would hold the port twice.",
    "sin_family is at offset one, not zero. Offset zero is sin_len on this family, and a shim written from memory of another platform reads it as the family.",
    "Binding something already listening or connected is an error in the interface and is refused.",
]
known_by = "published"
cites = "POSIX.1-2008 bind(2). struct sockaddr_in from sys/netinet/in.h."
found_in = [
    "klogsrv",
    "ftpsrv",
    "shsrv",
]
found_on = "2026-08-29"

[[function]]
name = "clock_gettime"
arity = 2
purpose = "Answers a named clock to the nanosecond. Declared here because a title imports it here; implemented in orbistoun-libc, which is where the C library lives."
returns = "status"
edge_cases = [
    "There is no vendor-named twin, so the delegation names the same function on both sides. The declaration and the implementation being in different crates is ordinary; two declarations of one symbol would not be.",
    "The behaviour is described by the libc entry of the same name - which clocks are answered, and why the rest are refused rather than given the nearest thing.",
]
known_by = "published"
cites = "POSIX.1-2008 clock_gettime(3). Identifiers from sys/sys/_clock_id.h, harvested."
found_in = [
    "ftpsrv",
    "websrv",
]
found_on = "2026-08-29"

[[function]]
name = "close"
arity = 1
purpose = "The POSIX spelling of `sceKernelClose`, failing the POSIX way."
returns = "status"
edge_cases = [
    "Served by `sceKernelClose`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "FAILS THE POSIX WAY, MEASURED (obSCEne 019-posixerr/provoked-errors): close(-1) answers -1 with errno = EBADF (9), where the vendor twin answers 0x8002_0009. The twin's vendor code is translated - errno set, -1 returned.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "connect"
arity = 3
purpose = "Connects a socket to an address."
returns = "status"
edge_cases = ["Only a pending socket may be connected; one already listening or connected is refused."]
known_by = "published"
cites = "POSIX.1-2008 connect(2)."
found_in = ["ftpsrv"]
found_on = "2026-08-29"

[[function]]
name = "creat"
arity = 2
purpose = "create a file for writing, truncating an existing one."
known_by = "published"
cites = "POSIX.1-2008 creat(2), which defines it as exactly open(path, O_WRONLY | O_CREAT | O_TRUNC, mode)."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "fdatasync"
arity = 1
purpose = "as [fsync], without promising to flush metadata."
edge_cases = ["Reference: POSIX.1-2008 fdatasync(2). Flushing metadata as well is more than the call promises and is therefore conforming; the reverse would not be."]
known_by = "published"
cites = "POSIX.1-2008 fdatasync(2)."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "fstat"
arity = 2
purpose = "What a guest is told about an open descriptor."
returns = "status"
edge_cases = [
    "The layout is a setting, not a constant. The FreeBSD checkout these are read from is a newer release than the target's user space, and this is one of the two structures that changed shape in between - so both layouts are taken from that one header and ORBISTOUN_STAT_LAYOUT chooses, defaulting to the older (D374).",
    "A socket and a standard stream answer nothing: neither has a size or a modification time, and reporting one would be inventing both.",
    "The behaviour is otherwise the libc entry for `stat`, which is where the fields are described.",
]
known_by = "published"
cites = "POSIX.1-2008 fstat(2); the structure from sys/sys/stat.h."
found_in = ["ftpsrv"]
found_on = "2026-08-29"

[[function]]
name = "fsync"
arity = 1
purpose = "flush a file's contents to the storage behind it."
edge_cases = ["Reference: POSIX.1-2008 fsync(2). A real flush: the whole point of the call is that a caller learns its bytes have landed, so answering success without asking the operating system gives the assurance and none of the substance."]
known_by = "published"
cites = "POSIX.1-2008 fsync(2)."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "getpagesize"
arity = 0
purpose = "the size of a page, in bytes."
known_by = "published"
cites = "POSIX.1-2001 getpagesize(2)."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "getpeername"
arity = 3
purpose = "Answers who is at the other end of a connected socket."
returns = "status"
edge_cases = ["Declared in this library by inference rather than measurement: eight of its siblings were measured being imported from here by a title, and this one was not. A title connects rather than listens, so nothing in the corpus asks for it."]
known_by = "published"
cites = "POSIX.1-2008 getpeername(2)."
assumptions = ["That this library is where the platform exports it."]
found_on = "2026-08-29"

[[function]]
name = "getsockname"
arity = 3
purpose = "Answers where a socket actually ended up."
returns = "status"
edge_cases = ["Worth having rather than stubbing: a server that binds to port zero asks the system which port it got, and prints it. Answering a made-up number would put a wrong port in front of whoever is trying to connect."]
known_by = "published"
cites = "POSIX.1-2008 getsockname(2)."
found_in = ["ftpsrv"]
found_on = "2026-08-29"

[[function]]
name = "gettimeofday"
arity = 2
purpose = "Answers the wall clock to the microsecond. Declared here because a title imports it here; implemented in orbistoun-libc."
returns = "status"
edge_cases = ["As for `clock_gettime`: no vendor twin, so the delegation names the same function on both sides, and the libc entry of the same name is the one that describes the behaviour."]
known_by = "published"
cites = "POSIX.1-2008 gettimeofday(3). struct timeval from sys/sys/_timeval.h."
found_in = [
    "ftpsrv",
    "websrv",
    "ps5-app-dumper",
]
found_on = "2026-08-29"

[[function]]
name = "htonl"
arity = 1
purpose = "host byte order to network byte order, 32 bits."
edge_cases = ["Only the low thirty-two bits are meaningful. The argument arrives in a 64-bit register and the high half is whatever the caller last had there."]
known_by = "published"
cites = "POSIX.1-2008 htonl(3)."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "htons"
arity = 1
purpose = "host to network, 16 bits. POSIX.1-2008 htons(3)."
known_by = "published"
cites = "POSIX.1-2008"
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "inet_ntop"
arity = 4
purpose = "Renders an address as text."
returns = "pointer"
edge_cases = [
    "Only AF_INET is served. A family this cannot render is refused rather than printed as something plausible, because the caller prints what it gets.",
    "A destination too small is refused rather than truncated: half an address is a different address.",
    "The payloads import the underscored spelling __inet_ntop, which is FreeBSD's own name for it. Both are served and they are one function.",
]
known_by = "published"
cites = "POSIX.1-2008 inet_ntop(3). INET_ADDRSTRLEN is 16, from sys/netinet/in.h."
found_in = [
    "klogsrv",
    "ftpsrv",
    "shsrv",
]
found_on = "2026-08-29"

[[function]]
name = "inet_pton"
arity = 3
purpose = "text as an address."
edge_cases = [
    "The other direction, and the one a server needs before it can serve. zftpd opens a socket, sets an option on it, and then turns the address it means to bind into four bytes; with nothing answering that, it closed the socket and reported Init failed - which reads as a networking problem and was a missing parser (D385).",
    "The three answers are the whole contract: 1 parsed, 0 the text is not a valid address in this family, -1 the family itself is not one this knows, with EAFNOSUPPORT. A caller distinguishes them, so a parse failure must not be reported as a family failure.",
    "AF_INET and AF_INET6, the same two [inet_ntop] renders. For the four-byte form: dotted quad only - the classful short forms (inet_aton's a.b.c, a.b, a bare number) are inet_aton's and not inet_pton's, which is the difference between the two functions.",
    "Resolves to `__inet_pton`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 inet_pton(3)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "listen"
arity = 2
purpose = "Makes a bound socket accept connections - the call that makes a service visible."
returns = "status"
edge_cases = [
    "This is the one an external checker observes. A service reads as up the moment a guest has a listening socket on its port, because the check is a connect and nothing more.",
    "The backlog is not honoured: the host's listener chooses its own, and passing a guest's number through would be reporting a queue depth this cannot promise.",
    "Listening on a socket nobody bound is refused rather than given a port.",
    "Declared in this library by inference rather than measurement: eight of its siblings were measured being imported from here by a title, and this one was not. A title connects rather than listens, so nothing in the corpus asks for it.",
]
known_by = "published"
cites = "POSIX.1-2008 listen(2)."
assumptions = ["That this library is where the platform exports it. Its siblings were measured here; this one was not."]
found_in = [
    "klogsrv",
    "ftpsrv",
    "shsrv",
]
found_on = "2026-08-29"

[[function]]
name = "lseek"
arity = 3
purpose = "The POSIX spelling of `sceKernelLseek`, and the same function pointer. Declared 2026-08-29."
returns = "count"
edge_cases = [
    "Delegates to `sceKernelLseek`; its knowledge entry is the one that describes the arguments.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = ["That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform."]
found_in = ["ftpsrv"]
found_on = "2026-08-29"

[[function]]
name = "madvise"
arity = 3
purpose = "tell the system how a range will be used."
known_by = "published"
cites = 'POSIX.1-2008 posix_madvise(2): *"the advice is not binding"* and an implementation may ignore it.'
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "mmap"
arity = 6
purpose = "Maps pages of memory into guest address space. Reference: POSIX.1-2008 mmap(2), FreeBSD SYS_mmap 477."
returns = "pointer"
edge_cases = ["Returns MAP_FAILED (!0) on zero length or allocation failure."]
found_by = "published-standard"
known_by = "published"
cites = "POSIX.1-2008 mmap(2), FreeBSD SYS_mmap 477."
found_in = [
    "klogsrv",
    "ftpsrv",
    "shsrv",
]
found_on = "2026-08-31"

[[function]]
name = "mprotect"
arity = 3
purpose = "change the protection of a range already reserved."
edge_cases = ["Resolves to `sceKernelMprotect`, and this describes that function."]
known_by = "published"
cites = "POSIX"
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "munmap"
arity = 2
purpose = "The POSIX spelling of `sceKernelMunmap`, and the same function pointer. Declared 2026-08-29."
returns = "status"
edge_cases = ["Delegates to `sceKernelMunmap`; its knowledge entry is the one that describes the arguments."]
found_by = "static"
known_by = "assumed"
assumptions = ["That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform."]
found_in = [
    "ftpsrv",
    "shsrv",
]
found_on = "2026-08-29"

[[function]]
name = "ntohl"
arity = 1
purpose = "network to host, 32 bits. POSIX.1-2008 ntohl(3)."
known_by = "published"
cites = "POSIX.1-2008"
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "ntohs"
arity = 1
purpose = "network to host, 16 bits. POSIX.1-2008 ntohs(3)."
known_by = "published"
cites = "POSIX.1-2008"
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "open"
arity = 3
purpose = "The POSIX spelling of `sceKernelOpen`, failing the POSIX way."
returns = "handle"
edge_cases = [
    "Served by `sceKernelOpen`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "FAILS THE POSIX WAY, MEASURED (obSCEne 019-posixerr/provoked-errors): open of a missing path answers -1 with errno = ENOENT (2), not the vendor 0x8002_0002. The twin's vendor code is translated - errno set, -1 returned.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "posix_clock_gettime"
arity = 2
purpose = "a named clock, to the nanosecond."
edge_cases = [
    "Anything else - the per-process and per-thread CPU clocks, CLOCK_UPTIME, the second variants - is refused rather than answered with the nearest thing. A guest measuring its own CPU time and receiving wall time gets a number that looks right and is not, which is the failure this project refuses on principle (principle 3).",
    "Resolves to `clock_gettime`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 clock_gettime(3); identifiers from sys/sys/_clock_id.h."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_close"
arity = 1
edge_cases = ["Resolves to `sceKernelClose`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_fstat"
arity = 2
purpose = "the same, by descriptor."
edge_cases = ["Resolves to `fstat`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008 fstat(2)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_fsync"
arity = 1
purpose = "flush a file's contents to the storage behind it."
edge_cases = [
    "Reference: POSIX.1-2008 fsync(2). A real flush: the whole point of the call is that a caller learns its bytes have landed, so answering success without asking the operating system gives the assurance and none of the substance.",
    "Resolves to `fsync`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 fsync(2)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_ftruncate"
arity = 2
purpose = "the same, by descriptor."
edge_cases = ["Resolves to `ftruncate`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008 ftruncate(2)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_getpid"
arity = 0
purpose = "the process the guest is running in."
edge_cases = [
    "The host process id, which is true rather than invented: the guest runs inside this process, so this is its process id in every sense that can be checked from inside it. A made-up constant would be indistinguishable until something compared it against something else.",
    "Resolves to `getpid`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 getpid(3)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_gettimeofday"
arity = 2
purpose = "the wall clock, to the microsecond."
edge_cases = [
    "The timezone argument is ignored, as it must be. POSIX marks it obsolete and says that if it is not null the behaviour is unspecified; FreeBSD fills in a structure whose contents have been meaningless for decades. Writing something plausible there would be inventing data; leaving it is what every modern caller expects.",
    "Resolves to `gettimeofday`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 gettimeofday(3); struct timeval from sys/sys/_timeval.h."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_kevent"
arity = 6
purpose = "Applies the changes, then waits for something registered to be ready and reports it. Answers how many events were written, zero for a timeout, -1 for a descriptor that is not a queue."
edge_cases = [
    "The changes are applied before the wait, and their failures are reported first. A caller passing a bad change and a full event list gets the errors it earned rather than a wait it did not ask for - which is the order the interface documents.",
    "Resolves to `kevent`, and this describes that function.",
]
known_by = "published"
cites = "FreeBSD kevent(2)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_kqueue"
arity = 0
purpose = "a new event queue."
edge_cases = ["Resolves to `kqueue`, and this describes that function."]
known_by = "published"
cites = "FreeBSD kqueue(2)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_lseek"
arity = 3
edge_cases = ["Resolves to `sceKernelLseek`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_mkdir"
arity = 2
purpose = "creates one directory."
edge_cases = [
    "The mode is not applied, and that is stated rather than hidden. The host this runs on need not have POSIX permission bits at all, and writing a plausible permission somewhere would be a fact about nothing. What the mode is *for* - keeping a directory private - is not something a guest can check from inside this emulator.",
    "Resolves to `mkdir`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 mkdir(2)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_mmap"
arity = 6
purpose = "maps pages of memory into guest address space."
edge_cases = ["Resolves to `mmap`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008 mmap(2), FreeBSD SYS_mmap (477)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_mprotect"
arity = 3
purpose = "change the protection of a range already reserved."
edge_cases = ["Resolves to `sceKernelMprotect`, and this describes that function."]
known_by = "published"
cites = "POSIX"
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_munmap"
arity = 2
purpose = "unmaps a span from the guest's address space."
edge_cases = [
    "Refuses a null address, which is the case the probe checks from the failure side: answering success for an unmap of nothing would tell a guest its memory was released when it was not.",
    "Resolves to `sceKernelMunmap`, and this describes that function.",
]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_nanosleep"
arity = 2
purpose = "the same again, by structure."
edge_cases = ["Resolves to `nanosleep`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008 nanosleep(3); struct timespec from sys/sys/timespec.h."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_open"
arity = 3
purpose = "Write intent is honoured only under /data. The flags used to be ignored entirely, because the only writable place would have been the user's own title directory. With storage the installation owns, the answer is where rather than whether: a write mode under /data creates, and one under /app0 still opens for reading so a guest that asked for more than it needed is not stopped (D250)."
edge_cases = [
    "Write intent is honoured only under /data. The flags used to be ignored entirely, because the only writable place would have been the user's own title directory. With storage the installation owns, the answer is where rather than whether: a write mode under /data creates, and one under /app0 still opens for reading so a guest that asked for more than it needed is not stopped (D250).",
    "Resolves to `sceKernelOpen`, and this describes that function.",
]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pread"
arity = 4
purpose = "a read at an offset, leaving the position alone."
edge_cases = [
    "The position is the whole point. pread exists so two threads can read one file at once, and an implementation that seeks, reads and seeks back is not that - it is a race with extra steps.",
    "Resolves to `pread`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 pread(2)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_destroy"
arity = 1
purpose = "The block is leaked rather than freed, as every handle here is: a guest that destroys an attribute and then uses it gets a stale object rather than a fault into freed memory, which is the safer of two wrong behaviours while nothing tracks lifetimes."
edge_cases = ["Resolves to `scePthreadAttrDestroy`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_getdetachstate"
arity = 2
edge_cases = ["Resolves to `scePthreadAttrGetdetachstate`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_getguardsize"
arity = 2
edge_cases = ["Resolves to `pthread_attr_getguardsize`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_getinheritsched"
arity = 2
edge_cases = ["Resolves to `pthread_attr_getinheritsched`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_getschedparam"
arity = 2
edge_cases = ["Resolves to `scePthreadAttrGetschedparam`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_getschedpolicy"
arity = 2
edge_cases = ["Resolves to `pthread_attr_getschedpolicy`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_getscope"
arity = 2
edge_cases = ["Resolves to `pthread_attr_getscope`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_getstacksize"
arity = 2
purpose = "A size, so the out-parameter is pointer-width rather than an int."
edge_cases = [
    "A size, so the out-parameter is pointer-width rather than an int.",
    "Resolves to `scePthreadAttrGetstacksize`, and this describes that function.",
]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_init"
arity = 1
purpose = "allocates a thread attribute object."
edge_cases = ["Resolves to `scePthreadAttrInit`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_setdetachstate"
arity = 2
edge_cases = ["Resolves to `scePthreadAttrSetdetachstate`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_setguardsize"
arity = 2
purpose = "stored, so a later get reads it back."
edge_cases = ["Resolves to `scePthreadAttrSetguardsize`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_setinheritsched"
arity = 2
purpose = "stored, so a later get reads it back."
edge_cases = ["Resolves to `scePthreadAttrSetinheritsched`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_setschedparam"
arity = 2
edge_cases = ["Resolves to `scePthreadAttrSetschedparam`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_setschedpolicy"
arity = 2
purpose = "stored, so a later get reads it back."
edge_cases = ["Resolves to `scePthreadAttrSetschedpolicy`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_setscope"
arity = 2
edge_cases = ["Resolves to `pthread_attr_setscope`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_attr_setstacksize"
arity = 2
edge_cases = ["Resolves to `scePthreadAttrSetstacksize`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_cond_broadcast"
arity = 1
edge_cases = ["Resolves to `scePthreadCondBroadcast`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_cond_destroy"
arity = 1
edge_cases = ["Resolves to `scePthreadCondDestroy`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_cond_init"
arity = 2
purpose = "two arguments, and no name. See [posix_pthread_create]."
edge_cases = ["Resolves to `pthread_cond_init`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_cond_reltimedwait_np"
arity = 3
purpose = "a relative timed wait."
edge_cases = [
    "pthread_cond_reltimedwait_np(cond, mutex, reltime) - a relative timed wait.",
    "Resolves to `pthread_cond_reltimedwait_np`, and this describes that function.",
]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_cond_signal"
arity = 1
edge_cases = ["Resolves to `scePthreadCondSignal`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_cond_timedwait"
arity = 3
purpose = """abstime is an absolute deadline, not a duration, which is the half a caller's own code depends on: it computes "now plus a second" once and re-passes the same deadline around a spurious-wakeup loop, so treating it as a relative timeout restarts the clock every turn and the loop never ends. duration_until does the conversion, and a deadline already past becomes a zero wait rather than a negative one."""
edge_cases = ["Resolves to `pthread_cond_timedwait`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_cond_wait"
arity = 2
purpose = "Releases the guest mutex around the wait and retakes it after, which is what the interface promises. It is not atomic here, because the two objects are independent in this crate - a signal arriving in the gap is lost where on the platform it would not be. Recorded on the entry rather than left for a hang to reveal."
edge_cases = [
    "Releases the guest mutex around the wait and retakes it after, which is what the interface promises. It is not atomic here, because the two objects are independent in this crate - a signal arriving in the gap is lost where on the platform it would not be. Recorded on the entry rather than left for a hang to reveal.",
    "Resolves to `scePthreadCondWait`, and this describes that function.",
]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_condattr_destroy"
arity = 1
purpose = "The object is leaked rather than freed, exactly as the thread attribute's destroy does: a guest may destroy an attribute while a condition variable built from it is still live, and reclaiming here would leave that one reading freed memory. The storage is small and bounded by how many a guest initialises."
edge_cases = ["Resolves to `pthread_condattr_destroy`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_condattr_getclock"
arity = 2
edge_cases = ["Resolves to `pthread_condattr_getclock`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_condattr_getpshared"
arity = 2
edge_cases = ["Resolves to `pthread_condattr_getpshared`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_condattr_init"
arity = 1
purpose = "allocates a condition-variable attribute object."
edge_cases = ["Resolves to `scePthreadCondattrInit`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_condattr_setclock"
arity = 2
edge_cases = ["Resolves to `pthread_condattr_setclock`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_condattr_setpshared"
arity = 2
edge_cases = ["Resolves to `pthread_condattr_setpshared`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_create"
arity = 4
purpose = "Implementations this crate provides, by symbol name."
edge_cases = [
    'libScePosix mostly delegates a POSIX name straight to its vendor-named twin, and the arity is taken from the twin "so the two cannot disagree". They disagree. Three of the vendor calls end in a name the POSIX ones do not have:',
    "read_name following a wild pointer is correct behaviour - a guest that passes one gets the fault it would have got. The defect is upstream: the argument was never passed, and the honest fix is to not read it. Each of these supplies no name, which is exactly what the caller said.",
    "Resolves to `pthread_create`, and this describes that function.",
]
known_by = "published"
cites = "POSIX"
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_detach"
arity = 1
purpose = "says nobody will join this thread."
edge_cases = [
    "Recorded rather than acted on. Detaching is a promise about who cleans up, and every guest thread here is a host thread the runtime already cleans up when its body returns - so the promise is kept by construction and there is nothing for this to do but agree.",
    "Resolves to `pthread_detach`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 pthread_detach(3)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_equal"
arity = 2
purpose = "whether two thread identifiers name the same thread."
edge_cases = [
    "POSIX.1-2008. Non-zero for equal, zero for not - the opposite sense to the _np comparison functions and to strcmp, which is the mistake worth naming: a caller reading this as a difference gets every answer backwards.",
    "Resolves to `pthread_equal`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008"
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_exit"
arity = 1
purpose = "ends the calling thread, and never comes back."
edge_cases = [
    "On the main thread this ends the program, and that is reported as the guest exiting deliberately - a different outcome from a fault, and the distinction that matters most in a report (D177).",
    "On any other thread it ends only that thread. Nothing here can unwind guest frames, so the thread is parked instead of returned from: it stops executing guest code, which is what pthread_exit promises, and its stack is not reclaimed, which is what this cannot do. A run has a time limit, so a parked thread costs the run nothing it was not already spending.",
    "Resolves to `pthread_exit`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 pthread_exit(3)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_getspecific"
arity = 1
purpose = "the calling thread's value for a key, or null if unset."
edge_cases = ["Resolves to `pthread_getspecific`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_join"
arity = 2
purpose = "Carries the joined thread's return value back through value. The guest thread function's return is in rax when its body returns; thread::spawn now keeps it and thread::join makes it available once the host thread ends, so a guest that returns a result from a thread and reads it after the join gets it rather than a zero (030-thread/join)."
edge_cases = ["Resolves to `scePthreadJoin`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_key_create"
arity = 2
purpose = "allocate a thread-specific-data key."
edge_cases = ["Resolves to `pthread_key_create`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_key_delete"
arity = 1
purpose = "retire a key. The binding in the calling thread is dropped; a retired id is not reused, so a stale use reads null rather than another key's value."
edge_cases = ["Resolves to `pthread_key_delete`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutex_destroy"
arity = 1
purpose = "The counterpart to Init, and the reason a run leaks lock objects without it. The conformance probe asks for it seventeen times in one run - more than any other missing entry - because it builds and tears down a lock per check (D256)."
edge_cases = ["Resolves to `scePthreadMutexDestroy`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutex_init"
arity = 2
purpose = "two arguments, and no name. See [posix_pthread_create]."
edge_cases = ["Resolves to `pthread_mutex_init`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutex_lock"
arity = 1
edge_cases = ["Resolves to `scePthreadMutexLock`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutex_timedlock"
arity = 2
edge_cases = ["Resolves to `pthread_mutex_timedlock`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutex_trylock"
arity = 1
purpose = "The one that must not answer OK when it fails. lock blocks until it has the mutex, so success is the only interesting answer; trylock exists precisely to report that it could *not* take it, and a guest branches on that. A stub reporting success would send it into a critical section it does not hold - which is worse than the missing implementation it replaced, because nothing in the trace would say so (principle 3)."
edge_cases = ["Resolves to `scePthreadMutexTrylock`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutex_unlock"
arity = 1
edge_cases = ["Resolves to `scePthreadMutexUnlock`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutexattr_destroy"
arity = 1
purpose = "The mutex attribute calls: Init, Settype, Setprotocol, Destroy."
edge_cases = ["Resolves to `scePthreadMutexattrDestroy`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutexattr_getprioceiling"
arity = 2
edge_cases = ["Resolves to `pthread_mutexattr_getprioceiling`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutexattr_getprotocol"
arity = 2
edge_cases = ["Resolves to `scePthreadMutexattrGetprotocol`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutexattr_getpshared"
arity = 2
edge_cases = ["Resolves to `pthread_mutexattr_getpshared`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutexattr_gettype"
arity = 2
edge_cases = ["Resolves to `scePthreadMutexattrGettype`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutexattr_init"
arity = 1
purpose = "allocates an attribute object and hands back its address."
edge_cases = [
    "The argument is a pointer to a pointer, as it is for every object in this family: the guest declares ScePthreadMutexattr attr = NULL and passes &attr. Treating that as the object itself meant Settype overwrote the guest's pointer variable with a type value, and the round-trip that followed spun for fourteen million calls (D272).",
    "Resolves to `scePthreadMutexattrInit`, and this describes that function.",
]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutexattr_setprioceiling"
arity = 2
edge_cases = ["Resolves to `pthread_mutexattr_setprioceiling`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutexattr_setprotocol"
arity = 2
edge_cases = ["Resolves to `scePthreadMutexattrSetprotocol`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutexattr_setpshared"
arity = 2
edge_cases = ["Resolves to `pthread_mutexattr_setpshared`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_mutexattr_settype"
arity = 2
purpose = "and it is now actually stored."
edge_cases = ["Resolves to `scePthreadMutexattrSettype`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_once"
arity = 2
purpose = "runs an initialiser exactly once."
edge_cases = [
    "Reference: POSIX.1-2008 pthread_once(3). The routine takes no arguments and answers nothing, unlike the C++ runtime's _Execute_once next door, whose callback is InitOnce-shaped and reports success - so this cannot simply forward to it. The flag is marked done *after* the routine returns, which is what makes a routine that never returns leave the flag unset rather than recorded complete.",
    "Not yet serialised across threads, exactly as _Execute_once records of itself: two threads racing the same fresh flag could both run the initialiser. Nothing measured does, and a per-flag guard is the fix when something does.",
    "Resolves to `pthread_once`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 pthread_once(3)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_rwlock_timedrdlock"
arity = 2
edge_cases = ["Resolves to `pthread_rwlock_timedrdlock`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_rwlock_timedwrlock"
arity = 2
edge_cases = ["Resolves to `pthread_rwlock_timedwrlock`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_rwlockattr_destroy"
arity = 1
edge_cases = ["Resolves to `pthread_rwlockattr_destroy`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_rwlockattr_getpshared"
arity = 2
edge_cases = ["Resolves to `pthread_rwlockattr_getpshared`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_rwlockattr_gettype_np"
arity = 2
purpose = "the non-portable kind accessor."
edge_cases = ["Resolves to `pthread_rwlockattr_gettype_np`, and this describes that function."]
known_by = "published"
cites = "FreeBSD pthread_rwlockattr_settype_np(3)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_rwlockattr_init"
arity = 1
edge_cases = ["Resolves to `pthread_rwlockattr_init`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_rwlockattr_setpshared"
arity = 2
edge_cases = ["Resolves to `pthread_rwlockattr_setpshared`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_rwlockattr_settype_np"
arity = 2
purpose = "the non-portable kind setter."
edge_cases = ["Resolves to `pthread_rwlockattr_settype_np`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_self"
arity = 0
purpose = "Who is asking. The calling thread is *adopted* if the guest did not create it, because the process's first thread runs guest code without ever having been created and the guest still asks it this."
edge_cases = ["Resolves to `scePthreadSelf`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_setspecific"
arity = 2
purpose = "bind a value to a key for the calling thread."
edge_cases = ["Resolves to `pthread_setspecific`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pthread_yield"
arity = 0
purpose = "offer the processor to another thread."
edge_cases = ["Resolves to `pthread_yield`, and this describes that function."]
known_by = "published"
cites = "FreeBSD pthread_yield(3); sched_yield(2) is the POSIX spelling of the same thing."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pwrite"
arity = 4
purpose = "the same, writing."
edge_cases = ["Resolves to `pwrite`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008 pwrite(2)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_pwritev"
arity = 4
purpose = "[writev] at an explicit offset."
edge_cases = ["Resolves to `pwritev`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008 pwritev(2)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_read"
arity = 3
edge_cases = ["Resolves to `sceKernelRead`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_rename"
arity = 2
purpose = "moves a file within the guest's own storage."
edge_cases = [
    "Both ends are checked. A rename whose source is writable and whose destination is not would be a way to write outside the writable mount using a call that only looks like it reads - so the destination goes through the same two gates.",
    "Resolves to `rename`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 rename(2)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_rmdir"
arity = 1
purpose = "removes one empty directory."
edge_cases = ["Resolves to `rmdir`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008 rmdir(2)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_select"
arity = 5
purpose = "Read is asked of the descriptor: a listener with a connection waiting, a stream with bytes or an end-of-file, a file (always - a read from one does not block), and a standard stream (also always, because reading one here answers zero immediately rather than waiting on a terminal nobody is typing into)."
edge_cases = [
    "Read is asked of the descriptor: a listener with a connection waiting, a stream with bytes or an end-of-file, a file (always - a read from one does not block), and a standard stream (also always, because reading one here answers zero immediately rather than waiting on a terminal nobody is typing into).",
    "Write is answered yes for anything connected. Writes here go straight to the host and do not buffer, so a write will not block - and saying otherwise would park a guest waiting for a readiness that had already arrived.",
    "Exceptional is always empty. Nothing here generates out-of-band data or the other conditions that set it, so reporting one would be inventing an event.",
    "Resolves to `select`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 select(2); fd_set from sys/sys/select.h."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_sem_destroy"
arity = 1
purpose = "retire the semaphore a sem_t names."
edge_cases = ["Resolves to `sem_destroy`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_sem_getvalue"
arity = 2
purpose = "how many the semaphore has free."
edge_cases = [
    "Reference: POSIX.1-2008. Four bytes, because sval is an int * - a whole word would put the top half in whatever the guest keeps next door, which has bitten this crate twice (D210, D272).",
    "Resolves to `sem_getvalue`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_sem_init"
arity = 3
edge_cases = ["Resolves to `sem_init`, and this describes that function."]
known_by = "published"
cites = "POSIX unnamed semaphore, initialised in place."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_sem_post"
arity = 1
purpose = "give one back."
edge_cases = ["Resolves to `sem_post`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_sem_reltimedwait_np"
arity = 2
purpose = "the same wait, given a span instead."
edge_cases = ["Resolves to `sem_reltimedwait_np`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_sem_timedwait"
arity = 2
purpose = "take one, waiting until a deadline."
edge_cases = [
    "abstime is an absolute deadline, not a duration. Reading it as relative restarts the clock on every retry.",
    "Resolves to `sem_timedwait`, and this describes that function.",
]
known_by = "published"
cites = "POSIX.1-2008 sem_timedwait."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_sem_trywait"
arity = 1
purpose = "take one only if it is available."
edge_cases = ["Resolves to `sem_trywait`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_sem_wait"
arity = 1
purpose = "take one, waiting for it."
edge_cases = ["Resolves to `sem_wait`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_signal"
arity = 2
purpose = "Returns n, refusing to exceed the ceiling."
edge_cases = [
    "Refused rather than clamped. Silently capping would let a guest that has lost count carry on as though it had not, and the imbalance would surface as a hang somewhere with no connection to here (principle 3).",
    "Resolves to `signal`, and this describes that function.",
]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_sleep"
arity = 1
purpose = "waits, and really waits."
edge_cases = ["Resolves to `sleep`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008 sleep(3)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_stat"
arity = 2
purpose = "what a guest is told about a path."
edge_cases = ["Resolves to `stat`, and this describes that function."]
known_by = "published"
cites = "POSIX.1-2008 stat(2); the structure from sys/sys/stat.h, and which generation of it is a setting (D374)."
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "posix_write"
arity = 3
purpose = "A conformance probe reports by writing to standard output. One loader examined implements this purely as a filesystem call - it requires a real opened descriptor and refuses descriptor 1 - and that single choice is why it cannot emit a report at all (D170)."
edge_cases = [
    "A conformance probe reports by writing to standard output. One loader examined implements this purely as a filesystem call - it requires a real opened descriptor and refuses descriptor 1 - and that single choice is why it cannot emit a report at all (D170).",
    "Resolves to `sceKernelWrite`, and this describes that function.",
]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "preadv"
arity = 4
purpose = "[readv] at an explicit offset, leaving the file position alone."
known_by = "published"
cites = "POSIX.1-2008 preadv(2)."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_destroy"
arity = 1
purpose = "The POSIX spelling of `scePthreadAttrDestroy`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadAttrDestroy`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_attr_getdetachstate"
arity = 2
edge_cases = ["Resolves to `scePthreadAttrGetdetachstate`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_getguardsize"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_getinheritsched"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_getschedparam"
arity = 2
purpose = "The POSIX spelling of `scePthreadAttrGetschedparam`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadAttrGetschedparam`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_attr_getschedpolicy"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_getscope"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_getstacksize"
arity = 2
purpose = "A size, so the out-parameter is pointer-width rather than an int."
edge_cases = [
    "A size, so the out-parameter is pointer-width rather than an int.",
    "Resolves to `scePthreadAttrGetstacksize`, and this describes that function.",
]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_init"
arity = 1
purpose = "The POSIX spelling of `scePthreadAttrInit`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadAttrInit`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_attr_setdetachstate"
arity = 2
edge_cases = ["Resolves to `scePthreadAttrSetdetachstate`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_setguardsize"
arity = 2
purpose = "stored, so a later get reads it back."
edge_cases = ["Resolves to `scePthreadAttrSetguardsize`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_setinheritsched"
arity = 2
purpose = "stored, so a later get reads it back."
edge_cases = ["Resolves to `scePthreadAttrSetinheritsched`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_setschedparam"
arity = 2
purpose = "The POSIX spelling of `scePthreadAttrSetschedparam`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadAttrSetschedparam`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_attr_setschedpolicy"
arity = 2
purpose = "stored, so a later get reads it back."
edge_cases = ["Resolves to `scePthreadAttrSetschedpolicy`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_setscope"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_attr_setstacksize"
arity = 2
purpose = "The POSIX spelling of `scePthreadAttrSetstacksize`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadAttrSetstacksize`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_barrier_destroy"
arity = 1
edge_cases = ["Resolves to `scePthreadBarrierDestroy`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_barrier_init"
arity = 3
purpose = "the POSIX spelling, which takes no name."
edge_cases = ["Resolves to `posix_pthread_barrier_init`, and this describes that function."]
known_by = "published"
cites = "POSIX"
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_barrier_wait"
arity = 1
edge_cases = ["Resolves to `scePthreadBarrierWait`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_barrierattr_destroy"
arity = 1
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_barrierattr_getpshared"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_barrierattr_init"
arity = 1
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_barrierattr_setpshared"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_cond_broadcast"
arity = 1
purpose = "The POSIX spelling of `scePthreadCondBroadcast`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadCondBroadcast`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_cond_destroy"
arity = 1
purpose = "The POSIX spelling of `scePthreadCondDestroy`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadCondDestroy`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_cond_init"
arity = 2
purpose = "The POSIX spelling of `scePthreadCondInit`, one argument shorter. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "**Two arguments, not the three its vendor twin takes.** `scePthreadCondInit` ends in a name and the POSIX call has no argument for it, so delegating meant reading a register the caller never set - `zftpd` faulted on a stale `rdx` while initialising its client table (D385). It is written under its POSIX name in `orbistoun-kernel`, beside the vendor one it wraps with no name.",
    "Wraps `scePthreadCondInit` with an empty name; every other argument is that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_cond_reltimedwait_np"
arity = 3
purpose = "a relative timed wait."
edge_cases = ["pthread_cond_reltimedwait_np(cond, mutex, reltime) - a relative timed wait."]
known_by = "assumed"
assumptions = ["Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_cond_signal"
arity = 1
purpose = "The POSIX spelling of `scePthreadCondSignal`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadCondSignal`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_cond_timedwait"
arity = 3
purpose = """abstime is an absolute deadline, not a duration, which is the half a caller's own code depends on: it computes "now plus a second" once and re-passes the same deadline around a spurious-wakeup loop, so treating it as a relative timeout restarts the clock every turn and the loop never ends. duration_until does the conversion, and a deadline already past becomes a zero wait rather than a negative one."""
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_cond_wait"
arity = 2
purpose = "The POSIX spelling of `scePthreadCondWait`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadCondWait`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_condattr_destroy"
arity = 1
purpose = "The object is leaked rather than freed, exactly as the thread attribute's destroy does: a guest may destroy an attribute while a condition variable built from it is still live, and reclaiming here would leave that one reading freed memory. The storage is small and bounded by how many a guest initialises."
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_condattr_getclock"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_condattr_getpshared"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_condattr_init"
arity = 1
purpose = "allocates a condition-variable attribute object."
edge_cases = ["Resolves to `scePthreadCondattrInit`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_condattr_setclock"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_condattr_setpshared"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_create"
arity = 4
purpose = "The POSIX spelling of `scePthreadCreate`, one argument shorter. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "**Two arguments, not the three its vendor twin takes.** `scePthreadCreate` ends in a name and the POSIX call has no argument for it, so delegating meant reading a register the caller never set - `zftpd` faulted on a stale `rdx` while initialising its client table (D385). It is written under its POSIX name in `orbistoun-kernel`, beside the vendor one it wraps with no name.",
    "Wraps `scePthreadCreate` with an empty name; every other argument is that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_detach"
arity = 1
purpose = "Says nobody will join a thread. Recorded rather than acted on."
returns = "status"
edge_cases = [
    "Detaching is a promise about who cleans up, and every guest thread here is a host thread the runtime already cleans up when its body returns - so the promise is kept by construction and there is nothing to do but agree.",
    "A handle nobody issued is refused: an arbitrary guest value must never be treated as a thread.",
]
known_by = "published"
cites = "POSIX.1-2008 pthread_detach(3)."
found_in = ["ftpsrv"]
found_on = "2026-08-29"

[[function]]
name = "pthread_equal"
arity = 2
purpose = "whether two thread identifiers name the same thread."
edge_cases = ["POSIX.1-2008. Non-zero for equal, zero for not - the opposite sense to the _np comparison functions and to strcmp, which is the mistake worth naming: a caller reading this as a difference gets every answer backwards."]
known_by = "published"
cites = "POSIX.1-2008"
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_exit"
arity = 1
purpose = "Ends the calling thread, and never comes back."
returns = "status"
edge_cases = [
    "On the main thread it ends the program, reported as the guest exiting deliberately - a different outcome from a fault, and the distinction that matters most in a report (D177).",
    "On any other thread it ends only that thread, and the thread is parked rather than returned from: nothing here can unwind guest frames, so it stops executing guest code, which is what pthread_exit promises, and its stack is not reclaimed, which is what this cannot do.",
    "The two are told apart by the thread registry: the process's first thread runs guest code without ever having been created, so it is adopted rather than spawned.",
]
known_by = "published"
cites = "POSIX.1-2008 pthread_exit(3)."
assumptions = ["That a parked thread is close enough to an exited one for the payloads measured. None of them counts its own running threads."]
found_in = ["ftpsrv"]
found_on = "2026-08-29"

[[function]]
name = "pthread_getconcurrency"
arity = 0
purpose = "the concurrency level a guest asked for."
edge_cases = ['Reference: POSIX.1-2008. Zero unless the guest has set one, which the standard states outright: it is a hint to an implementation that multiplexes threads, this one does not, and answering zero says "the system decides" rather than inventing a level.']
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_getspecific"
arity = 1
purpose = "The calling thread's value for a key, or null if it was never set - which is what a fresh per-thread map answers, exactly as POSIX requires."
returns = "pointer"
known_by = "published"
cites = "POSIX.1-2008 pthread_getspecific(3)."
found_in = ["PPSA21564-app0"]
found_on = "2026-09-01"

[[function]]
name = "pthread_join"
arity = 2
purpose = "The POSIX spelling of `scePthreadJoin`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadJoin`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_key_create"
arity = 2
purpose = "Allocates a thread-specific-data key. A guest's own runtime (Intel TBB in a Unity title) builds its per-thread storage on these."
returns = "status"
edge_cases = [
    "Unimplemented it answered the placeholder, which TBB read as key allocation failing - it threw `TBB failed to initialize task scheduler TLS` and, with exceptions disabled, aborted (D453).",
    "The destructor argument is recorded nowhere and never run: nothing tears a guest thread down through this layer yet, so there is no moment for it to fire.",
]
known_by = "published"
cites = "POSIX.1-2008 pthread_key_create(3). Key is a small integer from a monotonic counter; value storage is per host thread, which is per guest thread here (D014)."
found_in = ["PPSA21564-app0"]
found_on = "2026-09-01"

[[function]]
name = "pthread_key_delete"
arity = 1
purpose = "Retires a key. A retired id is not reused, so a stale use reads null rather than another key's value."
returns = "status"
known_by = "published"
cites = "POSIX.1-2008 pthread_key_delete(3)."
found_in = ["PPSA21564-app0"]
found_on = "2026-09-01"

[[function]]
name = "pthread_mutex_destroy"
arity = 1
purpose = "The POSIX spelling of `scePthreadMutexDestroy`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadMutexDestroy`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_mutex_init"
arity = 2
purpose = "The POSIX spelling of `scePthreadMutexInit`, one argument shorter. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "**Two arguments, not the three its vendor twin takes.** `scePthreadMutexInit` ends in a name and the POSIX call has no argument for it, so delegating meant reading a register the caller never set - `zftpd` faulted on a stale `rdx` while initialising its client table (D385). It is written under its POSIX name in `orbistoun-kernel`, beside the vendor one it wraps with no name.",
    "Wraps `scePthreadMutexInit` with an empty name; every other argument is that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_mutex_lock"
arity = 1
purpose = "The POSIX spelling of `scePthreadMutexLock`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadMutexLock`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_mutex_timedlock"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_mutex_trylock"
arity = 1
purpose = "The one that must not answer OK when it fails. lock blocks until it has the mutex, so success is the only interesting answer; trylock exists precisely to report that it could *not* take it, and a guest branches on that. A stub reporting success would send it into a critical section it does not hold - which is worse than the missing implementation it replaced, because nothing in the trace would say so (principle 3)."
edge_cases = ["Resolves to `scePthreadMutexTrylock`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_mutex_unlock"
arity = 1
purpose = "The POSIX spelling of `scePthreadMutexUnlock`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadMutexUnlock`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_mutexattr_destroy"
arity = 1
purpose = "The POSIX spelling of `scePthreadMutexattrDestroy`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadMutexattrDestroy`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_mutexattr_getprioceiling"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_mutexattr_getprotocol"
arity = 2
edge_cases = ["Resolves to `scePthreadMutexattrGetprotocol`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_mutexattr_getpshared"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_mutexattr_gettype"
arity = 2
edge_cases = ["Resolves to `scePthreadMutexattrGettype`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_mutexattr_init"
arity = 1
purpose = "The POSIX spelling of `scePthreadMutexattrInit`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadMutexattrInit`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_mutexattr_setprioceiling"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_mutexattr_setprotocol"
arity = 2
purpose = "The POSIX spelling of `scePthreadMutexattrSetprotocol`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadMutexattrSetprotocol`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_mutexattr_setpshared"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_mutexattr_settype"
arity = 2
purpose = "The POSIX spelling of `scePthreadMutexattrSettype`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadMutexattrSettype`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_once"
arity = 2
purpose = "runs an initialiser exactly once."
edge_cases = [
    "Reference: POSIX.1-2008 pthread_once(3). The routine takes no arguments and answers nothing, unlike the C++ runtime's _Execute_once next door, whose callback is InitOnce-shaped and reports success - so this cannot simply forward to it. The flag is marked done *after* the routine returns, which is what makes a routine that never returns leave the flag unset rather than recorded complete.",
    "Not yet serialised across threads, exactly as _Execute_once records of itself: two threads racing the same fresh flag could both run the initialiser. Nothing measured does, and a per-flag guard is the fix when something does.",
]
known_by = "published"
cites = "POSIX.1-2008 pthread_once(3)."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlock_destroy"
arity = 1
edge_cases = ["Resolves to `scePthreadRwlockDestroy`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlock_init"
arity = 2
purpose = "the POSIX spelling, which takes no name."
edge_cases = [
    "pthread_rwlock_init(lock, attr) - the POSIX spelling, which takes no name.",
    "Resolves to `posix_pthread_rwlock_init`, and this describes that function.",
]
known_by = "published"
cites = "POSIX"
assumptions = [
    "The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlock_rdlock"
arity = 1
edge_cases = ["Resolves to `scePthreadRwlockRdlock`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlock_timedrdlock"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlock_timedwrlock"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlock_tryrdlock"
arity = 1
edge_cases = ["Resolves to `scePthreadRwlockTryrdlock`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlock_trywrlock"
arity = 1
edge_cases = ["Resolves to `scePthreadRwlockTrywrlock`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlock_unlock"
arity = 1
purpose = "Deliberately not [acquired], which the four acquiring calls share. There Some(false) means somebody else holds the lock, and Busy is the ordinary outcome of asking. Here it can only mean the caller released a lock nobody held - the release path has no contention branch - so answering Busy would name a cause this branch did not determine, and would send a guest into a retry loop over a bug in itself."
edge_cases = ["Resolves to `scePthreadRwlockUnlock`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlock_wrlock"
arity = 1
edge_cases = ["Resolves to `scePthreadRwlockWrlock`, and this describes that function."]
known_by = "assumed"
assumptions = [
    "Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it.",
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlockattr_destroy"
arity = 1
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlockattr_getpshared"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlockattr_gettype_np"
arity = 2
purpose = "the non-portable kind accessor."
known_by = "published"
cites = "FreeBSD pthread_rwlockattr_settype_np(3)."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlockattr_init"
arity = 1
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlockattr_setpshared"
arity = 2
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_rwlockattr_settype_np"
arity = 2
purpose = "the non-portable kind setter."
known_by = "assumed"
assumptions = ["Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_self"
arity = 0
purpose = "The POSIX spelling of `scePthreadSelf`, and the same function pointer. Declared 2026-08-27."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadSelf`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A guest importing both spellings reaches one implementation, so they cannot answer differently.",
    "A failure returns this project's placeholder rather than an errno. It deliberately avoids the high bit, so it can never be mistaken for an established value: a guest testing `!= 0` takes its error path correctly, and one switching on specific values falls to its default branch rather than matching the wrong case.",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "pthread_setcancelstate"
arity = 2
purpose = "Sets whether this thread may be cancelled, and hands back the state it replaced."
edge_cases = [
    "Arity two, read off the guest's own call: arg0 is 1 - disable, on FreeBSD - and arg1 a stack pointer for the previous value. PPSA02664 calls it twice, around a section it does not want interrupted (D561).",
    "The out-parameter is optional, as POSIX allows, and is an int - four bytes. Eight would take the caller's neighbouring variable with it (D272).",
    "One of the few in this family with a genuine POSIX analogue of the same name; D540's warning against assuming that does not apply here.",
]
known_by = "guest-observed"
assumptions = [
    "Stored and not acted on. orbistoun cancels no threads, so there is nothing for the state to gate - what a caller is promised is the previous value, and it gets that.",
    "The initial state is recorded as zero - PTHREAD_CANCEL_ENABLE on FreeBSD - because that is POSIX's default. Nothing measured says the console agrees.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-09-04"

[[function]]
name = "pthread_setconcurrency"
arity = 1
purpose = "records the hint so the getter can answer it."
known_by = "published"
cites = "POSIX.1-2008, which permits an implementation to ignore the value entirely."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pthread_getschedparam"
arity = 3
purpose = "The POSIX spelling of `scePthreadGetschedparam`, and the same function pointer."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadGetschedparam`: `(thread, int *policy, struct sched_param *)` in both spellings, and its knowledge entry is the one that describes them.",
    "A failure returns this project's placeholder rather than an errno, as every delegated POSIX name does (D349).",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_on = "2026-09-25"
note = "Served alongside pthread_setschedparam."

[[function]]
name = "pthread_setschedparam"
arity = 3
purpose = "The POSIX spelling of `scePthreadSetschedparam`, and the same function pointer."
returns = "status"
edge_cases = [
    "Delegates to `scePthreadSetschedparam`: `(thread, policy, const struct sched_param *)` in both spellings, and its knowledge entry is the one that describes them.",
    "A failure returns this project's placeholder rather than an errno, as every delegated POSIX name does (D349).",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "Which convention the platform's POSIX-named exports use on failure: POSIX's own - a returned errno, or -1 with errno set - or the vendor encoding `0x8002_0000 | errno` measured on their vendor-named twins (D398). One provoked failure on hardware settles it for this call, and a second in another family settles whether it is a convention at all.",
]
found_in = ["PPSA02664-app0", "PPSA04263-app0"]
found_on = "2026-09-03"
note = "Served by delegation."

[[function]]
name = "pthread_setspecific"
arity = 2
purpose = "Binds a value to a key for the calling thread."
returns = "status"
known_by = "published"
cites = "POSIX.1-2008 pthread_setspecific(3)."
found_in = ["PPSA21564-app0"]
found_on = "2026-09-01"

[[function]]
name = "pthread_yield"
arity = 0
purpose = "offer the processor to another thread."
known_by = "published"
cites = "FreeBSD pthread_yield(3); sched_yield(2) is the POSIX spelling of the same thing."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "pwritev"
arity = 4
purpose = "[writev] at an explicit offset."
known_by = "published"
cites = "POSIX.1-2008 pwritev(2)."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "read"
arity = 3
purpose = "The POSIX spelling of `sceKernelRead`, failing the POSIX way."
returns = "count"
edge_cases = [
    "Served by `sceKernelRead`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A failure is translated to -1 with errno set, the convention obSCEne measured for open and close through their POSIX names (019-posixerr/provoked-errors).",
]
found_by = "published-standard"
known_by = "assumed"
assumptions = [
    "That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform.",
    "That read fails like open and close, its measured siblings among the POSIX file calls - inferred, not provoked on hardware for read itself.",
]
found_in = ["PPSA02664-app0"]
found_on = "2026-08-27"

[[function]]
name = "readv"
arity = 3
purpose = "a read scattered across several buffers."
known_by = "published"
cites = "POSIX.1-2008 readv(2)."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "recv"
arity = 4
purpose = "Reads from a connected socket."
returns = "count"
edge_cases = [
    "The flags are ignored, as for send.",
    "Reading a listening socket is a guest's mistake, reported rather than answered with an empty read that looks like a closed connection.",
]
known_by = "published"
cites = "POSIX.1-2008 recv(2)."
found_in = [
    "ftpsrv",
    "elfldr",
]
found_on = "2026-08-29"

[[function]]
name = "sched_yield"
arity = 0
known_by = "published"
cites = "POSIX.1-2008, the same as [pthread_yield]."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "select"
arity = 5
purpose = "Waits until one of a set of descriptors is ready, or until a timeout."
returns = "count"
edge_cases = [
    "A server's loop is wait, accept, serve, and this is the wait. Without it the loop never turns: the guest is told the call failed and takes its error path having served nothing.",
    "Readiness is asked by polling, a millisecond apart, because the standard library has no readiness primitive. That costs latency a real select would not, and a guest measuring its own wakeup latency would see the difference.",
    "Asking must not consume. The only way to learn whether a listener has a connection is to accept one, so the connection is kept on the listener and the guest's own accept takes it - otherwise a guest that selects then accepts loses every connection to the call that only asked (D373).",
    "Write readiness is yes for anything connected: writes here go straight to the host and do not buffer, so a write will not block.",
    "The exceptional set is always cleared. Nothing here generates out-of-band data, so reporting one would be inventing an event.",
    "A null timeout means wait as long as it takes; a zeroed one means ask once and answer. A caller means quite different things by them and both are ordinary.",
]
known_by = "published"
cites = "POSIX.1-2008 select(2). fd_set from sys/sys/select.h: FD_SETSIZE is 1024 and __fd_mask is an unsigned long, so sixteen 64-bit words with descriptor n at bit n%64 of word n/64."
found_in = [
    "klogsrv",
    "ftpsrv",
]
found_on = "2026-08-29"

[[function]]
name = "sem_destroy"
arity = 1
purpose = "Retires the host semaphore a sem_t names."
returns = "status"
known_by = "published"
cites = "POSIX.1-2008 sem_destroy(3)."
found_on = "2026-09-01"

[[function]]
name = "sem_getvalue"
arity = 2
purpose = "how many the semaphore has free."
edge_cases = ["Reference: POSIX.1-2008. Four bytes, because sval is an int * - a whole word would put the top half in whatever the guest keeps next door, which has bitten this crate twice (D210, D272)."]
known_by = "published"
cites = "POSIX.1-2008."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "sem_init"
arity = 3
purpose = "Initialises a POSIX unnamed semaphore in place. Creates a host semaphore with the given initial value and writes its handle into the sem_t, mirroring how pthread_cond_init stores a handle in the guest's object."
returns = "status"
edge_cases = [
    "PPSA21564's engine builds a concurrency primitive on this; unimplemented it answered the placeholder, which the guest asserted was zero and then aborted (D455).",
    "pshared is ignored - a guest thread is a host thread here, so process-shared versus process-private makes no difference to a single-process emulator.",
]
known_by = "published"
cites = "POSIX.1-2008 sem_init(3); host primitive is orbistoun-kernel sync::create_semaphore, shared with sceKernelCreateSema."
found_in = ["PPSA21564-app0"]
found_on = "2026-09-01"

[[function]]
name = "sem_post"
arity = 1
purpose = "Gives one back to a POSIX semaphore."
returns = "status"
known_by = "published"
cites = "POSIX.1-2008 sem_post(3)."
found_on = "2026-09-01"

[[function]]
name = "sem_reltimedwait_np"
arity = 2
purpose = "the same wait, given a span instead."
known_by = "assumed"
assumptions = ["Derived from the implementation's own documentation, which cites no published specification. Nothing on the target has confirmed it."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "sem_timedwait"
arity = 2
purpose = "take one, waiting until a deadline."
edge_cases = ["abstime is an absolute deadline, not a duration. Reading it as relative restarts the clock on every retry."]
known_by = "published"
cites = "POSIX.1-2008 sem_timedwait."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."

[[function]]
name = "sem_trywait"
arity = 1
purpose = "Takes one only if available; a would-block and a bad handle are both non-zero, distinguished by errno on the platform, which this project does not invent."
returns = "status"
known_by = "published"
cites = "POSIX.1-2008 sem_trywait(3)."
found_on = "2026-09-01"

[[function]]
name = "sem_wait"
arity = 1
purpose = "Takes one from a POSIX semaphore, waiting for it. Reads the handle sem_init stored in the sem_t."
returns = "status"
known_by = "published"
cites = "POSIX.1-2008 sem_wait(3)."
found_on = "2026-09-01"

[[function]]
name = "send"
arity = 4
purpose = "Writes to a connected socket."
returns = "count"
edge_cases = ["The flags are ignored and that is stated: MSG_OOB and MSG_DONTROUTE are not things this can promise, and a guest relying on one would be misread. Every payload measured passes zero."]
known_by = "published"
cites = "POSIX.1-2008 send(2)."
found_in = ["ftpsrv"]
found_on = "2026-08-29"

[[function]]
name = "setsockopt"
arity = 5
purpose = "Accepted, and recorded as not applied."
returns = "status"
edge_cases = [
    "Accepting is the part that matters. A server's first act after socket is setsockopt(SO_REUSEADDR), and failing it stops the server: a correct program checks, reports and exits. Refusing outright would end every payload measured before it reached bind.",
    "Nothing is applied. SO_REUSEADDR is what the host's listener does by default on the platforms this runs on, so honouring it would change nothing; the rest - timeouts, buffer sizes, keepalive - would need a per-option mapping nothing here can verify, and a wrong one is a socket behaving differently from what the guest asked for with nothing saying so.",
]
known_by = "published"
cites = "POSIX.1-2008 setsockopt(2). SOL_SOCKET and SO_REUSEADDR from sys/sys/socket.h, harvested."
assumptions = ["That no payload measured depends on an option actually taking effect. They set SO_REUSEADDR and check the return value; none reads an option back."]
found_in = [
    "klogsrv",
    "ftpsrv",
    "shsrv",
]
found_on = "2026-08-29"

[[function]]
name = "shutdown"
arity = 2
purpose = "Stops one or both directions of a connected socket."
returns = "status"
edge_cases = ["A direction nobody defines is refused rather than treated as both."]
known_by = "published"
cites = "POSIX.1-2008 shutdown(2). SHUT_RD, SHUT_WR and SHUT_RDWR are 0, 1 and 2, from sys/sys/socket.h."
found_in = ["ftpsrv"]
found_on = "2026-08-29"

[[function]]
name = "socket"
arity = 3
purpose = "Answers a descriptor for a new socket, with nothing behind it yet."
returns = "handle"
edge_cases = [
    "Only AF_INET streams are served. Anything else is refused rather than quietly given a TCP socket: a guest asking for a datagram socket and receiving a stream would work for exactly as long as it took to send something.",
    "The descriptor comes from the same table files use, because a guest has one - close, read and write take either without being told which (D371).",
    "A socket exists before it has anything to do. The host makes a listener by binding and listening in one step, so a descriptor starts pending, remembers what bind was told, and becomes a host object at listen or connect.",
]
known_by = "published"
cites = "POSIX.1-2008 socket(2). AF_INET and SOCK_STREAM from sys/sys/socket.h, harvested."
found_in = [
    "klogsrv",
    "ftpsrv",
    "shsrv",
]
found_on = "2026-08-29"

[[function]]
name = "write"
arity = 3
purpose = "The POSIX spelling of `sceKernelWrite`, failing the POSIX way."
returns = "count"
edge_cases = [
    "Served by `sceKernelWrite`; the arguments are that function's, and its knowledge entry is the one that describes them.",
    "A failure is translated to -1 with errno set, the convention obSCEne measured for open and close through their POSIX names (019-posixerr/provoked-errors); that write follows its siblings is inferred.",
    "Seventeen of the twenty-five open-toolchain payloads import it - the POSIX spelling only. A server writes its output through this, so without it a payload that has accepted a connection has nothing to say on it.",
]
found_by = "static"
known_by = "assumed"
assumptions = ["That this library's POSIX spelling and the vendor-named function it resolves to are the same behaviour on the target rather than merely similar. Unmeasured - it is inferred from the names and from both being exported by one platform."]
found_in = [
    "klogsrv",
    "ftpsrv",
]
found_on = "2026-08-29"

[[function]]
name = "writev"
arity = 3
purpose = "a write gathered from several buffers."
known_by = "published"
cites = "POSIX.1-2008 writev(2)."
assumptions = ["The published specification is the source. Nothing on the target has confirmed that this platform's build follows it, and the failure convention in particular is unestablished."]
found_on = "2026-09-02"
note = "Derived from the implementation's doc comment by `orbistoun-gen knowledge`."
