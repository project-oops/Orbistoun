# System call numbers the target has that FreeBSD does not.
#
# **Hand-written, unlike `abi-constants.toml`, and that is the whole distinction.** That file
# is generated from FreeBSD headers and says so; a value typed into it by a person is one
# nobody can trace back to a source. These have no header to be traced to. They are numbers a
# *guest asked for*, read out of a running program, and each entry records how it came to be
# here so the two kinds of knowledge are never mixed in one table.
#
# Nothing may be added here because it seemed likely. The bar is that a guest was watched
# asking for the number, and that what it does with the answer is described.

[syscall]

# A guest asks for this before it will bring up its own runtime linker, and gives up when it
# gets nothing: `elfldr`, `pldmgr`, `klogsrv` and `shsrv` all stop here, print
# `Unable to initialize rtld`, and exit. It is the single call blocking every open-toolchain
# payload this project has tried.
#
# **Called as `(2, 8, out)`** - a kind, a length, and somewhere to put eight bytes. What comes
# back is a *pointer*, and the guest reads one field of what it points at: a sixteen-bit value
# at offset 0x16, which it shifts left sixteen and compares against 0x0700FFFF, 0x085FFFFF,
# 0x093FFFFF and 0x103FFFFF before choosing a code path.
#
# Those are firmware versions - 7.00, 8.50, 9.30, 10.30 - so the field is the version of the
# system the guest is running on, and the call is how it finds out. That reading is inferred
# from what the guest does with the answer rather than from any documentation, and it is
# checkable: a value in a different band sends the guest down a different branch, which a run
# can watch it take.
#
# Provenance: guest-observed. The number was read from four running programs; the shape of the
# answer was read from the instructions that consume it. See D403.
SYS_vendor_system_version = 649

# `sceKernelVirtualQuery`, by number rather than by name.
#
# The conformance probe's payload build calls this **4,213 times in one run** and gets `ENOSYS`
# to every one. What it is doing is legible from the arguments alone: the first is an address
# that walks *backwards* through the guest image in `0x4000` steps - `0x400000280000`,
# `0x40000027c000`, `0x400000278000` - which is a program mapping out its own address space one
# region at a time and never being told where a region ends, so it steps by hand and never stops.
#
# **The function is implemented here; only the number was missing.** `sceKernelVirtualQuery` has
# answered by name since the address space existed. A guest that asks for it as a name is served
# and a guest that asks for it as a number is refused, which is the same one-function-two-answers
# shape D632 found in `sceKernelDlsym` - and it is why an emulator that can answer this call
# watched a guest ask four thousand times and said nothing but `ENOSYS`.
#
# Provenance: guest-observed, and corroborated in source. The number was read from a running
# program's syscall trace; the sibling conformance-probe repository's own `runtime.c` issues
# `obs_invoke_syscall(603, address, flags, info, info_size)` under the name
# `sceKernelVirtualQuery`, which is this project's own code and so citable rather than inferred.
# See D641.
SYS_sceKernelVirtualQuery = 603

# The direct-memory trio, by number.
#
# **Reached only once 603 was bound**, which is what makes them observed rather than guessed:
# before that the probe's memory walk never got past `sceKernelVirtualQuery`, and these three
# numbers appeared in no trace. With the walk answered the guest asks for `572`, then `573`,
# then `574`, in that order - allocate, map, release - which is the shape of a program taking a
# span of direct memory, putting it somewhere, and giving it back.
#
# The sibling conformance-probe repository's `runtime.c` names all three, and names them
# *conditionally*: it issues `obs_invoke_syscall(572, …)` for `sceKernelAllocateDirectMemory`
# unconditionally, and picks `obs_is_ps5() ? 585 : 573` for map and `obs_is_ps5() ? 586 : 574`
# for release. **This guest asked for 573 and 574**, the lower pair - so under orbistoun its
# generation test answers "not the current one", which is a fact about what this emulator
# presents and is worth knowing on its own.
#
# **585 and 586 are deliberately absent.** They are the other half of that conditional and no
# guest here has asked for either. This file's bar is that a guest was watched asking, and
# adding a number because its sibling was observed is exactly the "it seemed likely" this
# table refuses.
#
# Provenance: guest-observed, corroborated in this project's own sibling source. See D641.
SYS_sceKernelAllocateDirectMemory = 572
SYS_sceKernelMapDirectMemory = 573
SYS_sceKernelReleaseDirectMemory = 574

# The kernel-log write, by number. A guest can reach the operator log by name through
# `sceKernelDebugOutText` or by this raw syscall - the one-function-two-answers shape D632/D641
# found for the resolver (`sceKernelDlsym`) and the memory query (`sceKernelVirtualQuery`).
#
# **Watched, with the operation it performs described.** A guest we wrote - the `dist` homebrew
# payload (oops-apps), built on the obSCEne/oops runtime - issues syscall 601 with first argument
# 7, recorded in its trace as an unserved direct call through the syscall gadget. What the number
# does is legible from the argument and from this project's own sibling source: obSCEne's
# `src/probe/runtime.c` and `src/probe/sections/posix.c` issue `obs_invoke_syscall(601, 7, buf,
# 0, …)` in three places, all as their raw log-write channel - posix.c even labels it
# `sys_call(SYS_klog, 7, marker_klog, …)`. So the first argument is an operation selector, `7`
# writes the NUL-terminated string at the second argument to the log, and success is any
# non-negative return (obSCEne does `if (n >= 0) return len`).
#
# The number was found in the aggregate census a `run` prints, which totals every module's trace,
# so it first read as the title being run rather than as the payload that actually issued it - a
# reminder that a direct syscall in that census belongs to whichever guest's trace carried it,
# not to whichever title the command named (worklog 727).
#
# **Served by the same function `sceKernelDebugOutText` is** (see that call's `SPELT_DIFFERENTLY`
# binding). That function writes the string at the second argument and ignores the first, which
# for the log call is a channel and for this is the selector - the same argument position,
# ignored the same way. The write (selector 7) is therefore served exactly; a selector other
# than 7 is not distinguished, which is the one simplification here, and it is the same one the
# log call already makes for its channel. Success is a plain 0, within the non-negative band
# obSCEne's caller accepts; the exact value the target answers is unmeasured, so 0 rather than a
# guessed byte count.
#
# **FreeBSD's 601 is `pdwait`, which is why it is here and not harvested.** The generated
# `abi-constants.toml` carries `SYS_pdwait = 601` from the FreeBSD header; the vendor kernel
# repurposes the number, so the harvested value is the wrong one for this target and this table -
# "numbers the target has that FreeBSD does not" - is exactly where the corrected one belongs.
#
# Provenance: guest-observed, corroborated in this project's own sibling source. The number and
# its argument were read from a running title's syscall trace; the operation was read from
# obSCEne's own runtime, which is citable rather than inferred. See D641 for the same basis.
SYS_vendor_klog = 601
