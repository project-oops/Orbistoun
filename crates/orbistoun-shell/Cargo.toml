[package]
publish = false
name = "orbistoun-shell"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "The system software: session lifecycle, guest-visible events, and console settings."

[lints]
workspace = true

[dependencies]
# The machine's identity is a domain type, shared with the layer that answers a
# guest from it (D394).
orbistoun-core = { workspace = true }
serde = { workspace = true }
toml = { workspace = true }
