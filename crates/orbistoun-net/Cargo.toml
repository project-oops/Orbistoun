[package]
publish = false
name = "orbistoun-net"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Networking HLE: declarations for the libraries a title imports."

[lints]
workspace = true

[dependencies]
orbistoun-hle = { workspace = true }

[dev-dependencies]
# Only the test needs it: the base itself is a plain `u32`, and the assertion that it
# reconstructs the measured codes goes through `GuestError::vendor_in` because that is the
# function a shim will actually call (D627).
orbistoun-core = { workspace = true }
