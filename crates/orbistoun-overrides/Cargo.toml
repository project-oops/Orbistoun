[package]
publish = false
name = "orbistoun-overrides"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Per-title overrides - one file per title, three layers merged per key, with provenance."

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
